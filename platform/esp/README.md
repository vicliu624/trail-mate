# `platform/esp`

ESP-specific platform implementations.

Current packages:

- `arduino_common`: shared Arduino-on-ESP runtime glue
- `arduino_audio_codec`: ESP Arduino audio codec platform package
- `boards`: shared ESP board abstractions, dispatcher glue, and reusable drivers
- `idf_common`: shared ESP-IDF runtime glue
