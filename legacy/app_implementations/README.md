# Legacy App Implementations

This directory contains transitional implementation roots previously located
under `apps/`.

They are not product app shells. Final app shells live under `apps/` and may
delegate here through explicit transitional adapters until the legacy roots can
be retired.
