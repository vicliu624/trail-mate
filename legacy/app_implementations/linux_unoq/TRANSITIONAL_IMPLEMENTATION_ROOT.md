# Transitional Implementation Root: linux_unoq

This directory currently contains a placeholder for a future UNO Q Linux
implementation.

It is not a final app shell semantic root.

Current path:

- `legacy/app_implementations/linux_unoq`

Original historical path:

- `apps/linux_unoq`

Final app shell:

- not yet promoted to an `apps/` product shell

Authoritative build wrapper:

- not yet promoted

Exit condition:

- target identity and build path are proven;
- a future product app shell owns the startup contract;
- this placeholder can be deleted or replaced by an explicit implementation
  adapter.
