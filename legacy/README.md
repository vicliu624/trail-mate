# Legacy

`legacy/` contains historical implementation roots and compatibility surfaces
that are still needed while product app shells converge.

New product app shells must not be added here. Use `apps/` for final,
future-facing app shell descriptors and `builds/` for build entrypoints.
