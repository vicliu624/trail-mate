# `apps/linux_sim`

First Linux validation target.

This shell will be used to prove that shared core modules can run outside ESP environments before Raspberry Pi and UNO Q hardware targets are introduced.
