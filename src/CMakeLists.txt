# Legacy note:
# The active ESP-IDF application component now lives in `apps/esp_idf/CMakeLists.txt`.
# This file is intentionally no longer used as an IDF component root, and `src/` now only keeps the thin PlatformIO Arduino trampoline entry (`src/main.cpp`).
